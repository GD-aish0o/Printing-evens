// Function definition
function calculateBill(priceofItem: number, quantity: number): number {
    // Calculate total bill amount
    let totalBill = priceofItem * quantity;
    
    // Return the total bill amount
    return totalBill;
}

// Calling the function
let price = 10; // Price per item
let quantity = 5; // Quantity purchased

// Call the function and store the result in a variable
let totalBill = calculateTotalBill(priceOfItem, quantityPurchased);


console.log("The total bill amount is:", totalBillAmount); 
// Output: The total bill amount is: 50